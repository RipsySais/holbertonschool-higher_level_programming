// Sélectionne le premier élément header trouvé dans le document
const headerElement = document.querySelector('header');

// Change la couleur du texte en rouge (#FF0000)
if (headerElement) {
  headerElement.style.color = '#FF0000';
}
